name = "hcdown-website"
main = "src/index.ts"
compatibility_date = "2025-01-01"

[[d1_databases]]
binding = "DB"
database_name = "hcdown"
database_id = "55eda029-ecd1-4751-a204-8e2a023b76f0"

# The authenticated /api/* routes (see docs/API.md) check keys stored in
# the api_keys D1 table above -- create/revoke them from /admin, no
# separate secret to configure here.
